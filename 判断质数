#include<stdio.h>
int prime (int n)
{
	int d;
	if(n==0||n==1)
	 return 0;
	 
if(n>2){


	for(d=2;d<n;d++)
	{
	   if(n%d==0)
	    {
		return 0;
	    }
	    else
	  {
	   return 1;
      }
    }
}
}

int main ()
{
	int n;
	int a ;
	scanf("%d",&a);
	n = prime(a);
	printf("%d",n);
}

